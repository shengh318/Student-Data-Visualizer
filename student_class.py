class student():
        def __init__(self,name, test_scores, quiz_scores, exit_survey_scores, before_test_scores) -> None:
                self.name = name
                self.test_scores = test_scores
                self.quiz_scores = quiz_scores
                self.exit_survey_scores = exit_survey_scores
                self.before_test_scores = before_test_scores